import React, {PureComponent} from 'react'
import styles from 'common/sass/Navbar'

class Navbar extends PureComponent {
  constructor() {
    super()
  }

  render() {
    return (
      <nav className={styles.navbar}>
      </nav>
    )
  }
}

export default Navbar
